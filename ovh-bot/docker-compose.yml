version: "3.8"

services:
  ovh-bot:
    image: gvddfdf/ovh-bot:latest
    container_name: ovh-bot
    restart: unless-stopped
    environment:
      - OVH_ENDPOINT=ovh-eu
      - OVH_APPLICATION_KEY=${OVH_APPLICATION_KEY}
      - OVH_APPLICATION_SECRET=${OVH_APPLICATION_SECRET}
      - OVH_CONSUMER_KEY=${OVH_CONSUMER_KEY}
      - OVH_ZONE=${OVH_ZONE:-IE}
      - TG_BOT_TOKEN=${TG_BOT_TOKEN}
      - TG_ALLOWED_USERS=${TG_ALLOWED_USERS}
      - TG_CHAT_ID=${TG_CHAT_ID}
    # 也可以用配置文件（二选一）
    # volumes:
    #   - ./config.toml:/app/data/config.toml:ro

  # 可选：监控模式
  # ovh-monitor:
  #   image: gvddfdf/ovh-bot:latest
  #   container_name: ovh-monitor
  #   restart: unless-stopped
  #   environment:
  #     - OVH_ENDPOINT=ovh-eu
  #     - OVH_APPLICATION_KEY=${OVH_APPLICATION_KEY}
  #     - OVH_APPLICATION_SECRET=${OVH_APPLICATION_SECRET}
  #     - OVH_CONSUMER_KEY=${OVH_CONSUMER_KEY}
  #     - OVH_ZONE=${OVH_ZONE:-IE}
  #     - TG_BOT_TOKEN=${TG_BOT_TOKEN}
  #     - TG_ALLOWED_USERS=${TG_ALLOWED_USERS}
  #     - TG_CHAT_ID=${TG_CHAT_ID}
  #   command: ["python3", "monitor.py", "ks-1-b", "ks-stor"]
